//function for adding numbers:
function getAdd(first: number, second: number){
    return first + second;
}
console.log(getAdd(4, 4));

//variable is used to store the information or data:
var person : string = "Saad";
console.log(person);